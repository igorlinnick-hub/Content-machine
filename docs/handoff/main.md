# Main — репозиторий, доступы, Drive-аккаунт, тексты и фото по нише

Обновлено: 2026-09-16 · ветка: main

## Состояние
**Рабочая копия — `~/Code/Content-machine`** (старый путь под `~/Documents` — симлинк). **Деплой по
`git push`**; `vercel --prod` руками не гонять — CLI грузит незакоммиченное. `.env.vercel.local` отдаёт
`[SENSITIVE]` и локально: прод-значения проверяются только по поведению. Прод — `content-machine-gules.vercel.app`,
`/api/version` отдаёт sha; админ-запрос = `Cookie: cm_admin=$ADMIN_KEY`, а `+ cm_view_as=<clinicId>` рендерит
экран глазами врача — так проверяется любая ролевая правка без второго устройства.

**Роли (16.09).** Скрипты генерит только админ: `/api/agents/generate` и `/refine` отвечают 403 клиничному
токену, вкладка Generate у врача скрыта. Видео врач **смотрит, но не скачивает**: ни чипов папок Drive,
ни «Open in Drive», плюс `copyRequiresWriterPermission` на файлах — аноним по прямой ссылке получает
«Can't download file», плеер и превью живут (проверено на проде).

**Drive приложения = `kinnil.official@gmail.com`**; корень записей `content-machine`, папка клиники
создаётся с anyone-reader. Замок скачивания — **право владельца**: записи по 22.08 принадлежат
`hellosystems111` и не лочатся, новые ложатся под kinnil и лочатся сами. Ретро-прогон и откат —
`POST /api/studio/recordings/fix-permissions?clinicId=…[&unlock=1]`, он же печатает владельца и
capability по каждому файлу.

**Compose-поллер** `com.hwc.canva-runner` (gui/501, тик 2 мин) ставится только
`bash scripts/canva-runner/install.sh`; `launchctl load` запрещён. **Canva:** серверный автофил мёртв,
карусели собирает Claude+MCP runner копированием мастера; живой реестр — `lib/posts/style-templates.ts`
(`lib/canva/templates.ts` — мёртвый код); refresh-токены одноразовые, живой в Supabase.

**Доступы:** `/c/<token>` + memorable-код, роли `doctor`/`editor` (editor везде трактуется как врач),
две клиники HWC. **Тексты:** формат = HOW, тема = WHAT; каталог форматов в файле — **9**, в проде **6**.
Формат теперь выбирается прямо на экране Generate (`FormatPicker` → `format` в теле запроса →
`formatOverride`, который в Writer был всегда). **Фото — доктрина по нише v4.1**; грабли Flux —
`POST-CRAFT.md §5a`. **Себестоимость:** карусель ≈ $11.8, скрипт ≈ $0.30.

**Yedino Systems** — бренд агентства отдельно от клиник: `docs/YEDINO-STYLE.md`, хендофф `yedino.md`.

## Последний заход
- Разделены роли врача и админа (см. Состояние) — пять коммитов, в проде, экран врача проверен через
  `cm_view_as`. Первая попытка замка соврала: ошибка Drive уходила в `.catch(() => {})`, роут рапортовал
  успех на незалоченных файлах. Теперь он читает флаг обратно и показывает владельца.
- Вычищена утечка заметок комплаенс-ревьюера в тело скрипта («A medical professional should confirm…»
  доехало до телесуфлёра и стояло в скрипте с оценкой 8.7). Промпт это и так запрещал — теперь это
  **проверяется**: `lib/agents/compliance-notes.ts` режет из вывода переписчика только им же добавленные
  предложения-инструкции. Библиотека обеих клиник почищена, сейчас ноль совпадений.
- Написан формат **`Patient question`** (ответ на возражение, бриф Yedino от 16.09) — единица контента
  один вопрос пациента, имя не «Objection» намеренно: слово тянет скрипт защищаться и обвинять.
- Три старые записи HWC мягко удалены из библиотеки (`status='deleted'`, обратимо); файлы в Drive живы —
  владелец `hellosystems111`, корзина и замок принадлежат ему.

## Сломано / не доделано
- **Незакоммичено и потому не в проде:** правки текстов 07.09 (`formats.ts`, `writer.ts`, `planner.ts`)
  и поверх них новый `Patient question`. Один коммит вынесет всё сразу — ждёт решения Игоря.
- **Ни один новый формат не прогонялся на живой модели** — `One thing`, `Vague vs specific`, `Patient question`.
- **Связи скрипта с вопросом нет.** В `scripts` места нет (`template_used` занят форматом). Предложено:
  таблица `clinic_objections` (номер, группа 1–5, текст, состояние находимости) + `scripts.objection_id`;
  она же даёт батч — планировщик берёт список вместо выдумывания тем. Ждёт список 34 вопросов от Игоря.
- **Миграция 055 (`canva_oauth_tokens`) не прогнана** — OAuth fail-soft'ит, в логе `token store unreadable`.
- **Canva MCP не авторизован** — подключение через настройки коннекторов claude.ai.
- Две карточки `cleaned.mp4` у HWC остались в библиотеке врача; удалить их существующим роутом нельзя —
  он сносит только `failed/processing/pending`.
- Розданные PDF-гайды указывают на старую папку записей; в `/videos` чипы считаются живьём.
- Коды врачей в истории public-репо — риск принят Игорем 03.09, заново не поднимать.
- Доступ «Anyone with the link» на папке формы снимать нельзя — на нём держатся врачебные ссылки.

## Следующий шаг
Закоммитить и задеплоить хвост 07.09 вместе с `Patient question`, затем прогнать по живому скрипту на
каждый из трёх непроверенных форматов и посмотреть, не сползает ли ответ на возражение в обвинение.
