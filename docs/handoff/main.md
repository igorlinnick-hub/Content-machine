# Main — репозиторий, доступы, Drive-аккаунт, тексты и фото по нише

Обновлено: 2026-09-17 · ветка: main

## Состояние
**Рабочая копия — `~/Code/Content-machine`** (старый путь в `~/Documents` — симлинк). **Деплой по `git push`**;
`vercel --prod` руками не гонять — CLI грузит незакоммиченное. Прод — `content-machine-gules.vercel.app`,
`/api/version` отдаёт sha; админ-запрос = `Cookie: cm_admin=$ADMIN_KEY`, а `+ cm_view_as=<clinicId>` рендерит
экран глазами врача — так проверяется любая ролевая правка без второго устройства. Локально приложение
**не поднять**: в `.env.local` нет ни `ADMIN_KEY`, ни ключей Supabase — UI-правки проверяются на проде.

**В репозитории работают две сессии одновременно** (эта и Yedino). `tsc` локально видит рабочую копию
целиком, а Vercel — только коммит: **коммитить файл, который правит другая сессия, = уронить сборку**.
Так и вышло 16.09 с `planner.ts`. Перед `git add` смотреть `git status` и брать только свои файлы.

**Роли (16.09).** Скрипты генерит только админ: `/api/agents/generate` и `/refine` отвечают 403 клиничному
токену, вкладка Generate у врача скрыта. Видео врач **смотрит, но не скачивает**: ни чипов папок Drive,
ни «Open in Drive», плюс `copyRequiresWriterPermission` на файлах — аноним по прямой ссылке получает
«Can't download file», плеер и превью живут (проверено на проде).

**Drive приложения = `kinnil.official@gmail.com`**; корень записей `content-machine`, папка клиники — с
anyone-reader. Замок скачивания — **право владельца**: записи по 22.08 за `hellosystems111` не лочатся,
новые лочатся сами. Ретро-прогон и откат — `POST /api/studio/recordings/fix-permissions?clinicId=…[&unlock=1]`.

**Compose-поллер** `com.hwc.canva-runner` ставится только `bash scripts/canva-runner/install.sh`
(`launchctl load` запрещён). **Canva:** серверный автофил мёртв, карусели собирает Claude+MCP runner
копированием мастера; живой реестр — `lib/posts/style-templates.ts`, refresh-токены одноразовые.

**Доступы:** `/c/<token>` + memorable-код, роли `doctor`/`editor`, две клиники HWC. **Тексты:** формат = HOW,
тема = WHAT; каталог форматов — **9**, выбор на экране Generate (`FormatPicker` → `format` →
`formatOverride`). **Возражения:** вопрос — строка в `clinic_objections` (056 прогнана), `objectionId`
или `"next"` в генератор, связь лежит в `scripts.objection_id`. **Фото — доктрина по нише v4.1**; грабли
Flux — `POST-CRAFT.md §5a`. **Себестоимость:** карусель ≈ $11.8 **с подписки** (`ANTHROPIC_API_KEY` в env
раннера не задан — вернуть его = ~$200/мес живых денег), скрипт ≈ $0.30 через API.

**Yedino Systems** — бренд агентства отдельно от клиник: `docs/YEDINO-STYLE.md`, хендофф `yedino.md`.

## Последний заход
- **Вопрос доведён до Writer'а данными.** Миграция 056 прогнана на живой базе; маршрут принимает
  `objectionId` (или `"next"` — первый вопрос без скрипта, это и есть съёмочный день), закреплённый вопрос
  сам включает формат, вытесняет тему из плана, прибивает первую строку дословно и разводит три варианта
  **по пациентам**, а не по услугам. Заведены пробные вопросы 101–103, номера 1–34 свободны.
- **Сборка падала сутки**: коммит `32f26fe` утащил `planner.ts`, переписанный сессией Yedino под
  `formatsForNiche`, без второй половины (экспорты в `formats.ts`). Файл откачен в коммите `5760195`,
  рабочая копия не тронута.
- Вычищена утечка заметок ревьюера в тело скрипта (`lib/agents/compliance-notes.ts` режет из вывода
  переписчика только им же добавленные предложения-инструкции); библиотеки обеих клиник почищены.
- Телесуфлёр получил **режим 16:9** (работа сессии Yedino, в рабочей копии): поворот телефона переключает
  раскладку сам, свой кегль 56 px с регулятором 28–120, панели ушли в overlay, позиция чтения хранится долей.

## Сломано / не доделано
- **Третий живой прогон `Patient question` не сделан** — ровно тот, что проверяет, стала ли первая строка
  вопросом и легла ли связь в `scripts.objection_id`. Прогон ждёт сборку `5760195`.
- **Списка 34 вопросов нет** — в таблице только три пробных. Метод и живой пример: `docs/objection-maps/`
  в репозитории Yedinosystems (править там нельзя, файлы за той сессией).
- **16:9 не проверен на реальном телефоне**: кегль 56 может быть мелким для штатива, и не проверено,
  пишет ли iOS дубль ландшафтом, а не крутит метаданные.
- **`One thing` и `Vague vs specific` в проде, но живьём не прогонялись.**
- **Миграция 055 (`canva_oauth_tokens`) не прогнана** — OAuth fail-soft'ит, в логе `token store unreadable`.
- **Canva MCP не авторизован** — подключение через настройки коннекторов claude.ai.
- Две карточки `cleaned.mp4` у HWC не удаляются существующим роутом (он сносит только `failed/processing/pending`);
  пять старых файлов HWC в Drive живы — владелец `hellosystems111`, корзина и замок принадлежат ему.
- Розданные PDF-гайды указывают на старую папку записей. Коды врачей в истории public-репо — риск принят 03.09.
- Доступ «Anyone with the link» на папке формы снимать нельзя — на нём держатся врачебные ссылки.

## Следующий шаг
Дождаться сборки `5760195` и прогнать `Patient question` через `objectionId` — проверить дословность первой
строки во всех трёх вариантах и запись `objection_id`. Затем принять от Игоря список вопросов в
`clinic_objections` (номера 1–34, пробные 101–103 удалить) и включить батч через `"next"`.
