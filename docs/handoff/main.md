# Main — доступы, телепромтер + /videos, брендирование входа, подбор фото для каруселей

Обновлено: 2026-08-19 · ветка: main

## Состояние
**Доступы.** Вход в прод (`content-machine-gules.vercel.app`) — ссылка `/c/<token>` или
memorable-код на экране входа, cookie на год. Выпуск/отзыв — только в UI `/clinics` →
`InstallLinkCard`. Роли: `doctor` (тонкая поверхность), `editor` (back-office). Клиник две,
обе HWC: Dr. Shawn `5065c6ee-7c4b-451b-8dee-3498eb3af674` (дефолт), Dr. Made
`cdb530e5-3ce9-4088-abee-117bc872e39a`. Коды/токены здесь намеренно НЕ записаны.

**Запись телепромтера.** `1920×1080 @ 30fps` через `ideal` (не `max` — портретный поток
деградирует), битрейт от фактических `getSettings()` (~0.13 бит/пиксель/кадр, 4–12 Мбит/с),
H.264/mp4 первым приоритетом (VP9 на телефоне роняет кадры, mp4 играется в Drive без
ремукса). 1080p — сознательный потолок. **С 2026-08-19 кнопка «Start Recording» в оверлее
заблокирована («Starting camera…»), пока нет стрима**: на компе вебкам греется 1–3 с и
браузер спрашивает разрешение — доктор жал в чёрный экран, `beginRecording` молча пропускал
`startRecording()`, текст скроллился без записи («Reading…» + «Not recording» + Exit, без
REC-таймера). Это было у всех докторов на компе, не per-doctor; на телефоне стрим успевал
раньше тапа. Теперь `startRecording()` возвращает boolean, без записи скролл не стартует,
есть `recorder.onerror`, при ошибке камеры — явная кнопка «Read without recording».

**UI телепромтера.** Транспорт (перемотка/пауза/скорость/стоп) внизу, в зоне большого
пальца; отбивка от плавающей адресной строки Safari — `.tp-controls-safe` в `globals.css`
(safe-area-inset её не учитывает). Скорость по умолчанию 25.

**`/videos` — библиотека видео для врача** (`app/videos/*`, с 2026-08-19). Табы
Recordings (`clinic_recordings` status=final) / Edited (`clips` status=cleaned; таб виден
только при наличии, открывается первым). Плеер — Drive-embed `…/file/<id>/preview`,
read-only, «Open in Drive». Карточка «My videos» на дашборде (не adminOnly), «Watch all →»
в телепромтере. Embed играет только файлы с доступом по ссылке — новые получают его в
`confirm` (`allowLinkView`), для старых есть `POST /api/studio/recordings/fix-permissions`.

**Брендирование HWC.** `/install` и `/` в стиле клиники (источник — `HWC-Landing-pages/`,
`Pain & Joint Landing 3/index.html`). Ассеты `public/brand/`, Playfair через `next/font`,
токены `--hwc-*`. Правило: логотип и текст на сплошном белом, никогда на фото.

**Форма генерации скриптов** свёрнута до выбора темы; в плановом пути летит `planTopicId`,
текст инпута на сервер не уходит (одобрено 2026-08-13).

**Подбор фото для каруселей — доктрина v4** (2026-08-17): `ai` — только 3D-медрендеры и
гавайская природа (AI-людей нет вообще); `clinic` ~40% из Drive-библиотеки; `stock`
(Pexels) ≤2 на пост и только под предмет, которого у клиники нет. Обложка без фото.
Порядок clinic-фото — LRU с кулдауном 30 дней (не сверху-вниз, не рандом), кулдаун
стартует после удачного compose (`markPhotosUsed`). Нехватка отдохнувших — ослабление с
warning в лог. Служебный аккаунт `content-machine-sa@jobflow-491621.iam.gserviceaccount.com`
читает папку `HWC Pictures Google` (`120xEMg5Zl47ZpD6Bd8q5QodHFEdndzNK`), обход на два
уровня даёт 137 файлов; перенос igor → kinnil отменён (владение не передаётся).

## Последний заход
- Починен desktop-баг «Not recording» в `TeleprompterView` (см. Состояние).
- Добавлена страница `/videos` + карточка на дашборде + ссылка из телепромтера.
- `tsc` по затронутым файлам чистый; **ESLint не дожил** (8+ мин на 8 ГБ, убит).
- Ничего не закоммичено: дерево смешанное от параллельных сессий (серверный рендер
  слайдов, clips-runner, шрифты, миграция 047) — коммитить только свои файлы точечно.
- ГРАБЛИ машины: `next dev`, `next lint`, полный `tsc` на 8 ГБ виснут/таймаутят;
  типы проверять через отдельный tsconfig с `include` только нужных файлов.

## Сломано / не доделано
- Телепромтер-фикс и `/videos` **не видели вживую** — ни на компе, ни на телефоне.
- Сквозной прогон доктрины v4 не проверен: `/api/posts/generate` требует админской
  сессии, ни одного поста с `source:"clinic"` ещё не собрано; библиотека Made не
  проиндексирована (докторский роут подставляет клинику сессии — нужен админский заход).
- Обложка стилей 1/4/Aesthetic — фото выбирает раннер, не библиотека (`canva_style`
  ставится PATCH-ем после генерации). Не регресс.
- `/api/visual/photo-recommend`, `/photo-override`, `/photo-thumb/<id>` не существуют —
  UI визуальных постов в этой части нерабочий; каруселям не нужны.
- `/install` вживую никто не видел (за doctor-сессией).

## Следующий шаг
Закоммитить точечно `app/teleprompter/components/TeleprompterView.tsx`, `app/videos/*`,
`app/dashboard/components/DashBento.tsx`, запушить и на Vercel-preview проверить с компа:
кнопка Start серая 1–3 с → красная → после Start горит `REC 0:00` и «Stop & Save»; затем
открыть `/videos` с телефона под доктором.
