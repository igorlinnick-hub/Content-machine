# Main — репозиторий, поллер, доступы, Drive-аккаунт, тексты и фото по нише

Обновлено: 2026-09-03 · ветка: main

## Состояние
**Рабочая копия — `~/Code/Content-machine`**, старый путь под `~/Documents` — симлинк. Файл, читающийся
пустым при ненулевом размере, проверять `ls -lO` на `dataless`.

**Деплой — по `git push`** (GitHub-интеграция Vercel, план **Hobby**, шесть cron-записей). Руками
`vercel --prod` не запускать: CLI грузит рабочую папку вместе с незакоммиченным.

**Drive приложения = `kinnil.official@gmail.com`** (107 ГБ). Корень записей = `content-machine`
(`1Zn1cerArg3p5E-oDQD0BYhv2gfXjRQEL` — папка `Recordings` внутри), файлы ложатся в
`Recordings/<клиника>`, папка клиники создаётся с anyone-reader. Записи по 22.08 остались за
hellosystems111 — читаются, кнопка удаления по ним откажет. `lib/google/recordings.ts` проверяет
корень `files.get` и при 404/403 уходит в `GOOGLE_DRIVE_FOLDER_ID`.

**Compose-поллер** `com.hwc.canva-runner` в `gui/501`, тик 2 мин; ставить только
`bash scripts/canva-runner/install.sh` (`--check` — здоровье), `launchctl load` запрещён. Сборка
25–40 мин, heartbeat стучит из фонового цикла `run.sh`; файлы при живой сборке менять через
`cp → .new && mv`. Источник SKILL.md — `~/.claude/skills/canva-compose-runner/`, не репо.
Canva notes только на чтение — caption кладётся комментарием к дизайну.

**Доступы:** `/c/<token>` + memorable-код, роли `doctor`/`editor`, две клиники HWC — Made
(aesthetics) и Shawn (regenmed). Профиль HWC после 054: 5 пилларов, 22 услуги, 20 deep-dive тем.
CTA-ключи ManyChat — `lib/seeds/cta-keywords.ts`, пул BINDING, двенадцать эстетических заведены в ManyChat.

**Тексты:** формат = HOW, тема = WHAT; каталог форматов — 6 (`lib/posts/formats.ts`).
Treatment explainer — единственный, кто приземляется на услугу (слайд 5, и больше нигде).
Evidence-слайд — PubMed через `lib/posts/studies.ts`.
**Фото — доктрина по нише (v4.1):** regenmed = 3D-рендеры + Гавайи; aesthetics = только реальные
фото, нелицевые зоны, клиника ≤25%. Грабли Flux — `POST-CRAFT.md §5a`, `SKILL.md §4`.

**Себестоимость (замерена 03.09, не оценка):** один compose карусели в раннере = **$11.8 в среднем**
(медиана $11.47, разброс $4.93–18.45, 14 прогонов из `~/.claude/projects/…canva-runner/`). Скрипт
через `/api/agents/generate` — **~$0.30** (writer Sonnet + critic Opus + compliance ×3). Пост без
Canva ≈ $0.5 LLM + $0.3 Replicate. То есть 17 постов/мес = ~$200 API, если в env раннера лежит
`ANTHROPIC_API_KEY` ([run.sh:159](../../scripts/canva-runner/run.sh#L159)); без ключа то же самое
съедает лимиты подписки Игоря. Львиная доля — итерации визуального ревью и перегенерация фото.

## Последний заход
- Считал экономику эксплуатации (только анализ, код не трогал): цифры выше.
- Подтверждено на своей шкуре: `.env.vercel.local` отдаёт `[SENSITIVE]` **и при локальном чтении** —
  замерить объём контекста из прод-Supabase из сессии нельзя, оценки промптов только по коду.
- Телесуфлёр и Drive-корень (`e72a618`), клипы (`8fcadb6`), floor-media (`ad8194d`) — в проде,
  с прошлого захода без изменений.

## Сломано / не доделано
- **Незакоммиченный `app/components/AdminLogin.tsx`** (+55 строк): форма админа открывается сразу на
  устройстве с меткой `cm.admin-device` в localStorage — чтобы менеджер паролей автозаполнял поле.
  Живьём не проверено, автор захода — не эта сессия. Доделать или откатить.
- **Розданные PDF-гайды указывают на старую папку записей** (`1q97nn…`) — новые записи туда не попадают.
  В `/videos` чипы считаются живьём и ведут куда надо, расходятся только напечатанные ссылки.
- `DRIVE_RECORDINGS_ROOT_FOLDER_ID` sensitive — записанное значение нельзя проверить чтением, только
  по поведению; снимается в дашборде Vercel.
- Переписанные на «только очередь» `/api/clips/process` и `/api/cron/clips-inbox` — в `git stash`
  (раннера нет, очередь копила бы строки молча). Модульный хендофф — `docs/handoff/clips.md`.
- `assets/hwc-logo.png` в репо не попал — режет блэнкет `*.png` в `.gitignore`; на него никто не ссылается.
- Коды врачей в истории public-репо — **риск принят Игорем 03.09**, заново не поднимать.
- Доступ «Anyone with the link» на папке формы снимать нельзя — на нём держатся врачебные ссылки.
- Первый штатный крон `/api/cron/floor-media` (07:00 UTC) ещё не отрабатывал.
- Canva MCP отвалился — просит авторизацию через настройки коннекторов claude.ai.

## Следующий шаг
Перегенерировать оба PDF-гайда с новым id папки записей — `node --env-file=.env.vercel.local
scripts/render-doctor-guide.mjs --clinic shawn|made --recordings-folder 1Zn1cerArg3p5E-oDQD0BYhv2gfXjRQEL`
— у врачей на руках версия со ссылкой в старое дерево, куда новые записи уже не попадают.
