'use client'

// Background gradient is handled by body { animation: body-gradient } in globals.css.
// This component is kept as a no-op so existing imports don't break.
export function GradientBg() {
  return null
}
